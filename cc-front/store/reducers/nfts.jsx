export const initialState ={
    list:['Oyster Perpetual','GMT Master','Submariner Date','Day date']
}

const reducer = (state = initialState, action = {}) => {
    switch (action.type) {

        default:
            return state
    }
}

export default reducer