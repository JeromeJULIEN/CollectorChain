module.exports = {
    async collectionsPage(req, res) {
        // const collections = req.body;
        return res.send('Route vers toutes les collections');
        // return res.send(collections);
    },
};
