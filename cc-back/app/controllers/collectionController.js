module.exports = {
    async collectionPage(req, res) {
        // const collections = req.body;
        return res.send('Route vers une collection');
        // return res.send(collections);
    },
};
